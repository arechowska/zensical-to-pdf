\documentclass[12pt, a4paper]{article}

% Fonts
\usepackage{fontspec}
\IfFontExistsTF{$mainfont$}{
  \setmainfont{$mainfont$}
}{
  \setmainfont{Liberation Sans}
}

% Page margins
\usepackage[
  left=$geometry-left$,
  right=$geometry-right$,
  top=$geometry-top$,
  bottom=$geometry-bottom$
]{geometry}

% Language
\usepackage[$babel-lang$]{babel}

% Headers and footers
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small $header-left$}
\fancyhead[R]{\small $header-right$}
\fancyfoot[R]{\small \thepage}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0.4pt}

% Hyperlinks
\usepackage[colorlinks=true, linkcolor=black, urlcolor=blue]{hyperref}

% Images
\usepackage{graphicx}
\usepackage{float}
\makeatletter
\def\maxwidth{\ifdim\Gin@nat@width>\linewidth\linewidth\else\Gin@nat@width\fi}
\def\maxheight{\ifdim\Gin@nat@height>\textheight\textheight\else\Gin@nat@height\fi}
\makeatother
\setkeys{Gin}{width=\maxwidth,height=\maxheight,keepaspectratio}

% Tables
\usepackage{longtable, booktabs, array, calc}
\newcommand{\real}[1]{#1}

% Long URL wrapping
\usepackage{url}

% Paragraph spacing
\setlength{\parskip}{6pt}
\setlength{\parindent}{0pt}

% Page break before top-level headings
\makeatletter
\renewcommand\section{\clearpage\@startsection{section}{1}{\z@}%
  {-6ex \@plus -1ex \@minus -.2ex}%
  {2.3ex \@plus.2ex}%
  {\normalfont\Large\bfseries}}
\makeatother

% Keep images on the same page
\floatplacement{figure}{H}

% TOC number column widths (for deep numbering like 20.1.10)
\usepackage{tocloft}
\tocloftpagestyle{fancy}
\setlength{\cftsecnumwidth}{2.0em}
\setlength{\cftsubsecindent}{0.5em}
\setlength{\cftsubsecnumwidth}{2.5em}
\setlength{\cftsubsubsecindent}{1.0em}
\setlength{\cftsubsubsecnumwidth}{3.8em}


% Pandoc 3.x compatibility
\newcommand{\pandocbounded}[1]{#1}
\newcounter{none}

% Code blocks
\usepackage{fancyvrb}
\DefineVerbatimEnvironment{Highlighting}{Verbatim}{fontsize=\small, breaklines=true}
\usepackage{color}
\newenvironment{Shaded}{\small}{}

% tight_list fix
$if(tight-list)$
\providecommand{\tightlist}{\setlength{\itemsep}{2pt}\setlength{\parskip}{0pt}}
$else$
\providecommand{\tightlist}{\setlength{\itemsep}{4pt}\setlength{\parskip}{2pt}}
$endif$

\begin{document}

% Cover page
\begin{titlepage}
  \vspace*{4cm}
  \begin{center}
    {\LARGE\bfseries $cover-title$\par}
    \vspace{1cm}
    {\large $cover-subtitle$\par}
    \vspace{2cm}
    {\normalsize $cover-org$\par}
    \vfill
    {\small $cover-date$}
  \end{center}
\end{titlepage}

% Table of contents
$if(toc)$
\tableofcontents
\thispagestyle{fancy}
\newpage
$endif$

% Body
$body$

\end{document}
